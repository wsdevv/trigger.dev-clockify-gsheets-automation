import {to_google_sheets} from '../src/modules/ToGSheets';

describe('Google Sheets Writing Test', () => {
    test('Appending a to a Google sheet with auth key', () => {
        expect(to_google_sheets)
    })
});